import type { DatedSeries } from "@/domain/registry";
import type { TaxPolicy } from "@/domain/deposit/types";

/**
 * Налогообложение вознаграждения по депозитам физических лиц.
 *
 * НЕ ПОДТВЕРЖДЕНО. По умолчанию налог НЕ удерживается — и это осознанный выбор:
 * включённый по умолчанию налог занизил бы доходность каждого вклада в приложении.
 * В интерфейсе это отдельный переключатель, помеченный как допущение.
 */
export const DEPOSIT_INTEREST_TAX: DatedSeries<TaxPolicy> = {
  key: "tax.depositInterest",
  label: "ИПН на вознаграждение по депозиту",
  unit: "ratio",
  description: "Ставка налога на доход физического лица по вознаграждению банковского вклада.",
  entries: [
    {
      value: { kind: "none" },
      effectiveFrom: "2024-01-01",
      confidence: "unverified",
      note:
        "Требует сверки с Налоговым кодексом РК. По умолчанию налог не удерживается; " +
        "если в вашем случае ИПН применяется, включите его в параметрах расчёта.",
    },
  ],
};
